#pragma once

// ── Public paged KV cache API ─────────────────────────────────────────────────
// Delegates to the full internal API from src/kvcache/.
// Include via: #include <gc/gc_kvcache.h>

#include "gc_status.h"
#include <gc_kvcache.h>
