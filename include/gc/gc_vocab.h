#pragma once

// ── Public vocab / tokenizer API ──────────────────────────────────────────────
// Delegates to the full internal API from src/vocab/.
// Include via: #include <gc/gc_vocab.h>

#include "gc_status.h"
#include <gc_vocab.h>
